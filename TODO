 - we currently poll all rooms at a fixed interval. We should use the
   streaming API, but that will probably mean using threads, one per
   room.

 - document campfire api objects

 - the modules probably need a better name than Campfire, which is too
   generic and likely to clash with other Campfire implementations

 - command line options to override config file location and config
   variables
